num = int(input())

is_prime = True

if num < 2:
    print('NO')
else:
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            is_prime = False
    if not is_prime:
        print('NO')
    else:
        print('YES')
