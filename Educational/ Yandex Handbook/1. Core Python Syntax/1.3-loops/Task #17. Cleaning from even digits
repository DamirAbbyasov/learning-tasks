num = int(input())
result = 0
multiplier = 1

while num > 0:
    digit = num % 10
    if digit % 2 != 0:
        result += digit * multiplier
        multiplier *= 10
    num = num // 10

print(result)
