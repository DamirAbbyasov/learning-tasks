password = int(input())

min_sum = ((password // 10 % 10) + (password % 10))

max_sum = ((password // 100) + (password // 10 % 10))

if min_sum < max_sum:
    print(f"{max_sum}{min_sum}")
else:
    print(f"{min_sum}{max_sum}")
