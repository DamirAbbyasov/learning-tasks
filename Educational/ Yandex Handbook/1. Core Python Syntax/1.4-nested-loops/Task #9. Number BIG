n = int(input())
result = ""

for _ in range(n):
    number = int(input())
    biggest = 0
    while number > 0:
        digit = number % 10
        if digit > biggest:
            biggest = digit
        number //= 10

    result += str(biggest)

print(result)
